{
  "name": "Used Car Price Predictor",
  "build": {
    "dockerfile": "../Dockerfile"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "ms-vscode-remote.remote-containers"
      ]
    }
  },
  "postCreateCommand": "/workspace/Miniforge3-Linux-x86_64.sh -b -p /opt/miniforge && echo 'export PATH=/opt/miniforge/bin:$PATH' >> ~/.bashrc && source ~/.bashrc && conda init bash && conda config --set auto_activate_base false"
}
